# Суммари по проекту War Of Constellations

## ✅ Уже реализовано
1. **Сетевое взаимодействие (Network)**:
   - Подключение к Unity Services (Authentication).
   - Создание и поиск лобби, подключение к лобби (`NetworkConnectionManager`).
   - Синхронизация курсоров игроков (`NetworkCursor`).

2. **UI (Интерфейс)**:
   - Основное меню: Панели для хоста, присоединения, лобби (`HostGamePanel`, `JoinGamePanel`, `LobbyPanel`).
   - Менеджер переключения панелей (`MainMenuPanelsManager`).
   - Игровой HUD: Отображение золота (`GameplayHUD`).

3. **Core (Ядро)**:
   - `Bootstrapper`: Загрузка сцены MainMenu.
   - `GameManager`: Управление состояниями игры (Waiting, Playing, Finished).

4. **Геймплей (Структура)**:
   - Данные карты: Классы `Node` (узлы) и `Constellation` (созвездия).
   - `PlayerController`: Базовая структура для хранения ID игрока и золота.
   - `MapController`: Точка входа для работы с картой.

## 🚧 Что осталось (Основные задачи)
1. **Генерация карты**: Логика процедурной генерации созвездий и связей.
2. **Игровая логика**:
   - Захват узлов и созвездий.
   - Производство юнитов/ресурсов.
   - Условия победы/поражения.
3. **Синхронизация геймплея**: Передача данных о состоянии карты и действиях игроков по сети (помимо курсора).
4. **Взаимодействие**: Реализация кликов по узлам для атаки/покупки.

## 📝 Описание скриптов-заглушек (Stubs)

### `Gameplay/Map/MapGenerator.cs`
- **Метод**: `GenerateMap()`
- **Статус**: Пустой. Содержит комментарий `// TODO: Implement procedural generation of graph`.
- **Назначение**: Здесь должен быть алгоритм создания графа созвездий.

### `Core/Bootstrapper.cs`
- **Метод**: `InitializeServices()`
- **Статус**: Пустой. Содержит `// TODO: Initialize generic services`.
- **Метод**: `LoadNextScene()`
- **Статус**: Вызов `SceneManager.LoadScene` закомментирован, чтобы избежать ошибок при отсутствии сцен.

### `Gameplay/Mechanics/PlayerController.cs`
- **Метод**: `GetClickPower()`
- **Статус**: Возвращает базовое значение. Комментарий `// TODO: Apply modifiers`.
- **Назначение**: Должен рассчитывать силу клика с учетом прокачки/баффов.

### `Gameplay/Map/MapController.cs`
- **Статус**: Минимальная реализация. Просто вызывает `GenerateMap()` на старте, который пока ничего не делает.
